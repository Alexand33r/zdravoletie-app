# Zdravoletie thesis project
# Python 3.11.x
# Install: pip install -r requirements.txt

# Core data and ML
# numpy/pandas/pyarrow pinned below this specific range: pandas>=2.3 defaults to
# Arrow-backed string columns, and pyarrow>=17 combined with numpy>=2.1 segfaults
# on Apple Silicon inside pandas/core/arrays/string_arrow.py's ArrowStringArray
# construction when pd.read_csv runs on a Streamlit ScriptRunner background thread
# (reproduced via streamlit.testing.v1.AppTest with real multipage navigation).
numpy==2.0.2
pandas==2.2.3
pyarrow==16.1.0
scikit-learn==1.8.0
scipy==1.17.1
joblib==1.5.3

# Visualisation
matplotlib==3.10.8
seaborn==0.13.2

# Progress bars
tqdm==4.67.3

# SHAP explanations (TreeExplainer used throughout)
shap==0.51.0

# Jupyter execution
notebook>=7.0.0
nbconvert>=7.0.0
ipykernel==7.2.0
ipywidgets>=8.0.0

# Streamlit dashboard (multi-page app with file upload and PDF export)
streamlit==1.57.0

# Data scraping (Data_Scraping.ipynb only)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Excel file I/O
openpyxl>=3.1.0
